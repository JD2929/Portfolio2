import ReactDOM from 'react-dom/client';
import App from './App';

import 'bootstrap/dist/css/bootstrap.min.css'; // check to make sure this is correct

ReactDOM.createRoot(document.getElementById('root')).render(<App />);

